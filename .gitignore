# Local agent & editor state
.claude/
.idea/
.vscode/

# Local-only config (review before committing; may contain machine-specific settings)
.mcp.json
.gitmodules

# Build artifacts (skill zip pour upload claude.ai — voir README §Déploiement)
*.zip
